namespace UpdateHub.Backend.Tests.TestSupport;

/// <summary>
/// Объединяет тесты контроллеров в одну группу с общим поднятым приложением.
/// </summary>
/// <remarks>
/// Поднять приложение стоит заметного времени, а держать несколько экземпляров
/// нельзя: все они работали бы с одним каталогом раздачи и одним файлом базы.
/// Тесты внутри группы выполняются последовательно — это гарантирует xunit, —
/// поэтому общее состояние им не мешает. Данные при этом каждый тест заводит
/// свои: логины и идентификаторы компьютеров не пересекаются.
/// </remarks>
[CollectionDefinition(Name)]
public class ApiCollection : ICollectionFixture<UpdateHubApplication>
{
    /// <summary>Имя группы, указываемое в атрибуте <c>Collection</c>.</summary>
    public const string Name = "Приложение целиком";
}
